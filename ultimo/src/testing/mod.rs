//! Testing utilities for Ultimo applications.
//!
//! Enable with `ultimo = { version = "…", features = ["testing"] }` under
//! `[dev-dependencies]`. The [`TestClient`](crate::testing) drives your app
//! in-process — no socket, fully deterministic.
//!
//! See the per-type docs and the testing guide for usage examples.
