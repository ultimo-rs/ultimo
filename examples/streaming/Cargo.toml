[package]
name = "streaming-example"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
ultimo = { path = "../../ultimo" }
tokio = { version = "1", features = ["full"] }
futures-util = "0.3"
hyper = "1"
